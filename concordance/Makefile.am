bin_PROGRAMS = concordance
concordance_SOURCES = concordance.c
concordance_LDFLAGS = -lconcord
# extra warnings needed to ensure we're compatible with crappy
# Microsoft compilers - phil
concordance_CFLAGS = -Wall -ansi -Wdeclaration-after-statement

